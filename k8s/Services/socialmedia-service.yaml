apiVersion: v1
kind: Service
metadata:
  name: social-media-service
  namespace: socialmedia
spec:
  selector:
    app: social-media
  ports:
  - port: 8000
    targetPort: 8000
    nodePort: 30080
    protocol: TCP
  type: NodePort
